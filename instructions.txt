Una sola volta all'inizio, nella cartella principale del progetto, esegui i comandi:
	npm init -y
	npm install express socket.io

Fai partire il server con il comando:
	node server.js

In un browser connettiti a
    http://localhost:3000